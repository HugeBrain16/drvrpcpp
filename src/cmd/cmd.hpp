#include "lib/sampgdk.h"
#include "lib/cmd.h"

#include "limits.hpp"
#include "globals.hpp"
#include "dialog.hpp"
#include "files.hpp"
#include "utils.hpp"

extern bool cmd_ssveh(int playerid, Cmd cmd);
extern bool cmd_getmaterials(int playerid, Cmd cmd);
extern bool cmd_storage(int playerid, Cmd cmd);
extern bool cmd_rconweapon(int playerid, Cmd cmd);
extern bool cmd_setskin(int playerid, Cmd cmd);
extern bool cmd_money(int playerid, Cmd cmd);
extern bool cmd_tmt(int playerid, Cmd cmd);
extern bool cmd_beadmin(int playerid, Cmd cmd);
extern bool cmd_noadmin(int playerid, Cmd cmd);
extern bool cmd_job(int playerid, Cmd cmd);
extern bool cmd_me(int playerid, Cmd cmd);
extern bool cmd_do(int playerid, Cmd cmd);
extern bool cmd_sweeper(int playerid, Cmd cmd);
extern bool cmd_engine(int playerid, Cmd cmd);
extern bool cmd_rentveh(int playerid, Cmd cmd);
extern bool cmd_lockrv(int playerid, Cmd cmd);
extern bool cmd_mower(int playerid, Cmd cmd);
extern bool cmd_createhouse(int playerid, Cmd cmd);
extern bool cmd_createbiz(int playerid, Cmd cmd);
extern bool cmd_enter(int playerid, Cmd cmd);
extern bool cmd_exit(int playerid, Cmd cmd);
extern bool cmd_buy(int playerid, Cmd cmd);
extern bool cmd_inventory(int playerid, Cmd cmd);
extern bool cmd_buyhouse(int playerid, Cmd cmd);
extern bool cmd_lockhouse(int playerid, Cmd cmd);
extern bool cmd_saveplayer(int playerid, Cmd cmd);
extern bool cmd_speedlimit(int playerid, Cmd cmd);
